---
import { serverRules } from '../data/rules';
---

<div class="card">
  <div class="mb-6">
    <h2 class="text-2xl font-bold text-white mb-2">📜 Reglas del Servidor</h2>
    <p class="text-gray-400 text-sm">Lee y respeta estas reglas para mantener una comunidad saludable</p>
  </div>

  <div class="space-y-4">
    {serverRules.map((rule) => (
      <div 
        class:list={[
          "rounded-lg p-4 border transition-all hover:scale-[1.02]",
          {
            'bg-red-900/20 border-red-700/30': rule.severity === 'critical',
            'bg-yellow-900/20 border-yellow-700/30': rule.severity === 'important',
            'bg-blue-900/20 border-blue-700/30': rule.severity === 'info',
          }
        ]}
      >
        <div class="flex items-start space-x-3">
          <div 
            class:list={[
              "flex-shrink-0 w-8 h-8 rounded-full flex items-center justify-center font-bold text-sm",
              {
                'bg-red-500/20 text-red-400': rule.severity === 'critical',
                'bg-yellow-500/20 text-yellow-400': rule.severity === 'important',
                'bg-blue-500/20 text-blue-400': rule.severity === 'info',
              }
            ]}
          >
            {rule.id}
          </div>
          <div class="flex-1">
            <h3 
              class:list={[
                "font-semibold mb-1",
                {
                  'text-red-300': rule.severity === 'critical',
                  'text-yellow-300': rule.severity === 'important',
                  'text-blue-300': rule.severity === 'info',
                }
              ]}
            >
              {rule.title}
            </h3>
            <p class="text-gray-300 text-sm leading-relaxed">
              {rule.description}
            </p>
          </div>
          {rule.severity === 'critical' && (
            <div class="flex-shrink-0">
              <svg class="w-5 h-5 text-red-400" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd"></path>
              </svg>
            </div>
          )}
        </div>
      </div>
    ))}
  </div>


</div>

